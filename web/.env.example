# Default: this computer only. Set 0.0.0.0 for a trusted LAN, then open the computer's LAN IP on your phone.
HOST_BIND=127.0.0.1
HOST_PORT=8080
YT_DLP_VERSION=2026.8.19
# For an HTTPS reverse proxy, set both values (no trailing slash).
PUBLIC_ORIGIN=
COOKIE_SECURE=false
